# .github/workflows/release.yml

name: 🚀 Build & Release FontMuse

on:
  push:
    tags: ['v*']
  workflow_dispatch:

jobs:
  release:
    name: Build & Release on ${{ matrix.platform }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: ubuntu-latest
            target: x86_64-unknown-linux-gnu
          - platform: windows-latest
            target: x86_64-pc-windows-msvc
          - platform: macos-latest
            target: x86_64-apple-darwin
          - platform: macos-latest
            target: aarch64-apple-darwin

    runs-on: ${{ matrix.platform }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v2
        with:
          version: 8
      
      - name: Install frontend dependencies
        run: pnpm install

      # --- КЛЮЧЕВОЕ ИЗМЕНЕНИЕ ---
      # Используем tauri-action, который сам установит все зависимости и соберет проект
      - name: Build the app
        uses: tauri-apps/tauri-action@v0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          # Указываем, что нужно собрать portable-версию
          args: --target ${{ matrix.target }} --config src-tauri/tauri.portable.conf.json
          # Имя тега для релиза
          tagName: ${{ github.ref_name }}
          # Название релиза
          releaseName: 'FontMuse ${{ github.ref_name }}'
          # Описание релиза
          releaseBody: 'See the assets to download this version.'
          # Создавать как черновик
          releaseDraft: false
          # Это не пре-релиз
          prerelease: false